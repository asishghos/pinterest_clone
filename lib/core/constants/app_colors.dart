import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFFE60023);
  static const Color primaryDark = Color(0xFFAD081B);
  static const Color primaryLight = Color(0xFFFF4444);

  // Backgrounds & Surfaces
  static const Color background = Color(0xFF111111); // main dark bg
  static const Color surface = Color(0xFF1A1A1A); // cards, sheets
  static const Color surfaceVariant = Color(0xFF2A2A2A); // dividers, borders

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB3B3B3);
  static const Color textDisabled = Color(0xFF666666);

  // Brand & Accent
  static const Color pinterestRed = Color(0xFFE60023); // primary CTA
  static const Color pinterestRedLight = Color(0xFFFF3B30); // pressed/hover
  static const Color linkBlue = Color(0xFF00ADEF);

  // Status Colors
  static const Color success = Color(0xFF4CD964);
  static const Color warning = Color(0xFFFF9500);
  static const Color error = Color(0xFFD0021B);
  static const Color info = Color(0xFF0079D3);

  // Icons
  static const Color iconPrimary = Color(0xFFFFFFFF);
  static const Color iconSecondary = Color(0xFFB3B3B3);
  static const Color iconDisabled = Color(0xFF666666);

  // Overlays & Effects
  static const Color overlay = Color(0x99000000); // dialogs, modals
  static const Color overlayLight = Color(0x40000000);

  // Border Colors
  static const Color border = Color(0xFFCDCDCD);
  static const Color borderLight = Color(0xFFEFEFEF);

  // Shadow
  static const Color shadow = Color(0x1A000000);
}
